1  apt update -y
    2  python3 --version
    3  sudo apt install software-properties-common
    4  sudo apt-add-repository --yes --update ppa:ansible/ansible
    5  apt update -y
    6  apt install ansible -y
    7  ansible --version
    8  cd /etc/ansible
    9  ls -l
   10  vi hosts
   11  ansible -m ping host1
   12  exit
   13  cd /etc/ansible/
   14  ls -l
   15  vi hosts
   16  ansible -m ping host1
   17  ansible -m ping host2
   18  ansible -m ping dev
   19  ansible dev -m ping
   20  pwd
   21  cd ~
   22  pwd
   23  ls -la
   24  cd .ssh
   25  ls -l
   26  cat authorized_keys 
   27  ssh-keygen
   28  ls -l
   29  cat id_rsa.pub
   30  cd /etc/ansible/
   31  ls -l
   32  ansible -m ping dev
   33  history
   
Ansible used to install Apache on Host1 & Nginx on Host2
======================
firstrunbook.yaml with below code

---
- hosts: Host1
  become: yes
  name: Play1
  tasks:
  - name: Execte command 'Date'
    command: date
  - name: Install httpd
    apt: name=apache2 state=latest
- hosts: Host2
  become: yes
  name: Play2
  tasks:
  - name: Install httpd
    apt: name=nginx state=latest
